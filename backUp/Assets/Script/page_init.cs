using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class page_init : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Page_control.pagenum = 0; 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
