using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class repair_station : MonoBehaviour
{
    public static int station_level=1;
    public static int[] storage = new int[4] { 0, 500, 1000, 1500 };
    public static int[] speed = new int[4] { 0, 5, 4, 3 };

    public static int repair_doll;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
